<div class="row">
    <div class="col-sm-12">
        <h2>Index</h2>

        {{#if session.isAuthenticated}}
          <p> Welcome, {{session.user.username}}</p>
          <a href="#" {{ action 'invalidateSession' }}>Logout</a>
        {{else}}
          {{login-panel}}
        {{/if}}
    </div>
</div>

